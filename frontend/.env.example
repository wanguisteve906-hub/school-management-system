# Copy to .env in this folder:  Copy-Item .env.example .env
#
# npm run dev: the app talks to the API via Vite’s proxy (see vite.config.js) — no CORS setup needed.
#
# npm run build (production): set the public API URL:
# VITE_API_BASE_URL=https://your-api.example.com
